# pubspec.yaml
name: the_smart_umrah_project
description: A smart application for Umrah pilgrims and agents (UI Only).

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # Adjust based on your Flutter SDK version

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/umrah_app_logo.jpeg # <--- VERIFY THIS PATH AND FILENAME!